## Qué cambia

<!-- Una o dos frases. -->

## Por qué

<!-- Qué problema real de un evento resuelve. -->

## Checklist

- [ ] `pytest` en verde
- [ ] `ruff check src tests scripts` en verde
- [ ] Si toqué la segmentación: hay un test nuevo en `tests/test_segmenter.py`
- [ ] Si cambia algo visual: adjunté captura o GIF
- [ ] Si cambia latencia o costo: adjunté números antes/después (`scripts/scale_test.py`)
- [ ] Si agregué una variable de entorno: está en `.env.example` y en `Settings`
